## guidance: Add a "continue watching" row to the home screen.
status=CONFIDENT mode=['create'] platform=['tv'] input=['remote'] product=['media'] screen=['home'] stack=['compose', 'compose-tv'] density=medium env=['large-display', 'shared-device'] risk=low negatives=[]
MISSING: brand: no brand assets, guideline, or character description available
budget=moderate mode evidence: create: build/create request on an existing surface
project context: navigation={'value': 'tv-rails', 'status': 'KNOWN', 'evidence': ['tv-rails: 100 matches in AppState.kt, Cards.kt, EventDetailsScreen.kt (shell/layout file)']}, theme={'value': 'dark-first', 'status': 'KNOWN', 'evidence': ['dark theme configuration signals: 1', 'root/canvas backgrounds: 0 light, 1 dark', 'hex palette: 2 near-white, 4 near-black']}, surfaces={'value': 'bordered-flat', 'status': 'INFERRED', 'evidence': ['borders in 8 files, shadow/elevation in 1']}, radius={'value': 'pill', 'status': 'INFERRED', 'evidence': ['most common radius 50 (6×); others [8.0, 4.0]']}, spacing={'value': 8, 'status': 'INFERRED', 'evidence': ['most used spacing values [16, 10, 8]']}, typography={'value': 'custom', 'status': 'KNOWN', 'evidence': ['font family System (5 refs)', 'weights bold, semibold, normal, medium', 'encoded type scale in 1 files']}, components={'value': 'compose, compose-tv-material, media3, images:coil', 'status': 'KNOWN', 'evidence': ['compose', 'compose-tv-material', 'media3']}
concepts required=0.88 covered (7/8); tokens≈1352 coverage/1k=5.18 purity=0.83
concerns required=['structure', 'navigation', 'states', 'interaction', 'accessibility', 'performance'] covered=1.0 uncovered=[] recommended=['anti-pattern', 'brand', 'content', 'environment'] bundle=8 (core 4 + guardrails 4) diversity=1.0 redundancy=0.55
uncovered required concepts (no record carries them for this context): state.loading_empty_error

### CORE (what to build)
- **Media card (poster/thumbnail)** `comp-media-card` [accessibility/component/content] — Fixed aspect, image with placeholder + title fallback, title below (1–2 lines, ellipsis), one status overlay max (progress bar, live badge, new), whole card is one focusable/tappable element with an accessible name (title + status), TV focus = scale + border/glow, hover on web = subtle lift, touch = pressed state; no inner buttons on TV. _(covers: media card with one focus target and one status overlay, accessible names and labels)_
  - compose-tv: androidx.tv.material3 Card/StandardCardContainer with scale/border in focused state.
  - selected for: critical concept (DIRECT): media card with one focus target and one status overlay; lexical 0.713, structural 0.86
- **Media details screen, resume playback and watchlist** `media-resume-and-details` [component/interaction/navigation/states] — Details: the primary action is Play (or Resume with the remaining time and a Start over alternative) and it takes default focus; metadata is a short scannable block (duration, year, rating, badges as text not colour), synopsis ≤3 lines with an expander, episodes as a rail or list with progress bars and the next unwatched episode preselected; secondary actions (watchlist, trailer, more like this) sit after Play in one row. Resume: a continue-watching row shows progress on each card, resumes at the saved position, and removes finished items; entering a title from the row returns focus to that card. Watchlist: one toggle with a clear on/off state and text label, works from cards and details, and is reflected immediately in the watchlist row. Everything is reachable with D-pad UP/DOWN/LEFT/RIGHT and BACK returns to the row that launched the details. _(covers: details screen with Play as default focus, continue watching / resume playback, watchlist / save for later, focus restoration, BACK behaviour, no colour alone for status)_
  - compose-tv: focusRequester on the Play button in the details Column; LazyRow with focusRestorer for episodes; remember the launching card key to restore focus on BACK.
  - selected for: critical concept (SPECIFIC): continue watching / resume playback; lexical 0.894, structural 0.92
- **Cinematic media (TV)** `dir-cinematic-media-tv` [brand/structure] — Backdrop-driven home, side navigation, landscape rails with focus scale + glow, focus-revealed metadata, dark tinted canvas, a heavy display face for titles, filled icons at ≥32 dp, cinematic but debounced crossfades, and a player with transient controls. Identity via backdrop treatment (scrim shape, grain), display type, and the focus glow colour.
  - selected for: task evidence: product; lexical 0.029, structural 0.92
- **TV rail (horizontal row of cards)** `comp-tv-rail` [component/navigation/performance] — Rail title (≥24 sp) left-aligned in the safe area, cards of one aspect ratio, focused card scrolls to a fixed pivot (~10–30% from left) with LEFT at index 0 going to navigation, focus memory per rail, lazy loading of items and images, 'see all' as the last card if the rail is capped, no wrap-around, consistent card counts per width (Android: ~4 landscape / ~6 portrait at 960 dp). _(covers: focus restoration, focus latency, media card with one focus target and one status overlay)_
  - compose-tv: LazyRow inside LazyColumn with focusRestorer() and LocalBringIntoViewSpec for pivot; item keys stable; contentPadding = safe margins.
  - selected for: required coverage (GENERIC): performance; lexical 0.351, structural 1.0

### CRITICAL GUARDRAILS (must hold)
- **Reuse → extend → compose → new (in that order)** `impl-reuse-before-new` [structure; engineering-practice; GENERIC] — Inspect the repository first (inspect_project.py): existing components, tokens, fonts, breakpoints, and conventions win. Reuse the existing component; if it lacks a variant, extend it through its API; if the composition is new, compose existing primitives; only create a new primitive when the gap is real, and put it where the others live. _(covers: reuse → extend → compose → new)_
  - selected for: critical concept (GENERIC): reuse → extend → compose → new
- **TV: 10-foot typography** `tv-typography-distance` [accessibility/content; platform-standard; SPECIFIC] — Body ≥24 sp (Android) / ≥29 pt (tvOS) at 1080p design scale, captions ≥20 sp, titles 32–48, display 57–72; sans with large x-height and open counters; short strings (titles ≤2 lines, synopsis ≤3 lines with expansion); avoid thin weights (<400) and light text on busy imagery; line height ≥1.3. _(covers: 10-foot typography, readable at distance)_
  - selected for: required coverage (SPECIFIC): 10-foot typography, readable at distance
- **TV: vertical = sections, horizontal = items** `tv-dpad-axes` [interaction/navigation; platform-standard; SPECIFIC] — Every focusable element must be reachable with straight UP/DOWN/LEFT/RIGHT presses; no diagonal reasoning, no hidden hops. Search and settings live at a predictable edge. Grids: LEFT at the first column may enter side navigation, RIGHT at the last column stays. Forms: one field per row, DOWN advances. _(covers: D-pad focus reachability)_
  - selected for: required coverage (SPECIFIC): D-pad focus reachability
- **TV: exactly one visible focus at all times** `a11y-tv-focus-always` [accessibility/component/interaction; platform-standard; SPECIFIC] — Set initial focus deterministically (first actionable content or Play on detail), restore focus to the previously focused item when returning, keep focus on screen (scroll into view), move focus to a sensible neighbour when the focused item is removed, and never rely on colour tint alone for the focused state. _(covers: focus restoration, visible focus, details screen with Play as default focus)_
  - selected for: required coverage (SPECIFIC): visible focus
Filtered out: anti-mobile-desktop-shrunk (platform ['mobile'] not in request ['tv']); anti-hover-only-actions (platform ['desktop', 'web'] not in request ['tv']); chart-heatmap-matrix (platform ['desktop', 'web'] not in request ['tv']); comp-data-table (platform ['desktop', 'web'] not in request ['tv']); comp-filters (platform ['desktop', 'mobile', 'web'] not in request ['tv']); comp-list-row-mobile (platform ['mobile', 'tablet'] not in request ['tv'])

Concept trace (explain): covered media.resume_playback [CRITICAL SPECIFIC], process.reuse_first [CRITICAL GENERIC], interaction.dpad_reachability, interaction.focus_restore, interaction.focus_visible, tv.ten_foot_typography, layout.media_card [CRITICAL DIRECT], interaction.back_semantics, perf.focus_latency
  - UNCOVERED state.loading_empty_error: candidates existed but the bundle cap or a lower utility left them out (candidates comp-mini-player, anti-no-states, layout-states-empty-loading-error)
  - UNCOVERED tv.no_touch_hover: candidates existed but the bundle cap or a lower utility left them out (candidates tv-no-touch-hover)
  - UNCOVERED tv.dark_first: candidates existed but the bundle cap or a lower utility left them out (candidates tv-dark-first)
  - UNCOVERED navigation.orientation_and_back: candidates existed but the bundle cap or a lower utility left them out (candidates nav-orientation-and-back)
  - UNCOVERED privacy.shared_device: candidates existed but the bundle cap or a lower utility left them out (candidates shared-device-privacy, comp-tv-sign-in)
  - UNCOVERED tv.safe_margins: candidates existed but the bundle cap or a lower utility left them out (candidates comp-mini-player, anti-desktop-scaled-to-tv, tv-safe-area)
