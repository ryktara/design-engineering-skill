## guidance: Add an offline banner and retry to the report list for when the site has no signal.
status=AMBIGUOUS mode=['create'] platform=['web'] input=['keyboard', 'pointer', 'touch'] product=['iot'] screen=[] stack=['flutter'] density=None env=['gloves', 'low-bandwidth', 'outdoor'] risk=low negatives=[]
MISSING: brand: no brand assets, guideline, or character description available
CONFLICTS: platform (request kept; repository platform recorded as context)
budget=moderate mode evidence: create: build/create request on an existing surface
project context: navigation={'value': 'top-bar', 'status': 'KNOWN', 'evidence': ['top-bar: 6 matches in checklist_screen.dart, defect_report_screen.dart, settings_screen.dart (shell/layout file)']}, theme={'value': 'dual-theme', 'status': 'KNOWN', 'evidence': ['dark theme configuration signals: 5', 'light theme configuration signals: 2', 'hex palette: 13 near-white, 6 near-black'], 'default': 'light'}, surfaces={'value': 'flat-tonal', 'status': 'UNKNOWN', 'evidence': ['no shadow or border declarations found']}, radius={'value': 'small', 'status': 'INFERRED', 'evidence': ['most common radius 4 (1×); others []']}, spacing={'value': 'irregular', 'status': 'UNKNOWN', 'evidence': ['most used spacing values [2, 88, 12]']}, typography={'value': 'custom', 'status': 'KNOWN', 'evidence': ['font family System (7 refs)', 'weights w600, w700, w400', 'encoded type scale in 2 files']}, components={'value': 'go_router, riverpod', 'status': 'KNOWN', 'evidence': ['go_router', 'riverpod']}
concepts required=0.62 covered (5/8); tokens≈1114 coverage/1k=4.49 purity=0.91
concerns required=['component', 'structure', 'states', 'interaction', 'accessibility', 'environment'] covered=1.0 uncovered=[] recommended=['navigation', 'anti-pattern', 'brand', 'adaptive', 'feedback', 'performance'] bundle=8 (core 3 + guardrails 5) diversity=0.88 redundancy=0.53
uncovered required concepts (no record carries them for this context): interaction.keyboard_navigation, interaction.hover_independence, env.outdoor_readability

### CORE (what to build)
- **Toast / snackbar / banner** `comp-toast-notification` [accessibility/component/feedback/states] — Toast: bottom (mobile) or bottom-left/top-right (desktop) consistent position, ≥5 s or until dismissed, undo where applicable, live region polite. Banner: inline at the top of the region it concerns, dismissible if non-critical. TV: brief overlay in the safe area that never steals focus. _(covers: live region status announcements)_
  - selected for: task evidence: component, lexical_strong; lexical 0.267, structural 0.4
- **Pagination vs infinite scroll vs load more** `comp-pagination` [component/data-display] — Tables and admin lists: numbered pagination with page size and total; feeds: load-more or infinite scroll with scroll restoration and a way to link to items; catalogues: load-more; TV rails: lazy append at the rail end. Pagination is a nav landmark with aria-current on the page. _(covers: pagination / load-more strategy)_
  - selected for: task evidence: component; lexical 0.091, structural 0.4
- **List rows** `card-list-row` [structure] — Row height from the density token (48–72 dp), whole row tappable with one accessible name, trailing chevron only when it navigates, swipe actions mirrored by a visible menu.
  - selected for: task evidence: lexical_strong; lexical 0.286, structural 0.52

### CRITICAL GUARDRAILS (must hold)
- **Offline, sync, and connectivity states** `states-offline-and-sync` [environment/feedback/states; engineering-practice; DIRECT] — Design the offline state as a first-class state: show connectivity clearly but calmly (a persistent status strip, not a blocking modal), keep already-loaded content usable, queue writes locally with a visible 'pending sync' marker per item and a 'last synced' timestamp, retry automatically with backoff and let the user retry manually, never lose entered data, and resolve conflicts explicitly (show both versions or last-writer-wins with an undo). Reads: stale data is labelled with its age. _(covers: offline and sync states, saving, saved and conflict states, last-updated / refresh state)_
  - selected for: critical concept (DIRECT): offline and sync states
- **Target size by platform** `a11y-target-size` [accessibility/interaction; accessibility-requirement; GENERIC] — Web: ≥24×24 CSS px minimum (WCAG 2.5.8), 44×44 recommended for touch. iOS ≥44 pt, Android ≥48 dp, kiosk ≥60 px, desktop pointer ≥24 epx with 4–8 px spacing. Extend the hit area beyond the visual glyph rather than enlarging the icon. _(covers: large touch targets (≥44–48 px))_
  - selected for: critical concept (GENERIC): large touch targets (≥44–48 px)
- **Reuse → extend → compose → new (in that order)** `impl-reuse-before-new` [structure; engineering-practice; GENERIC] — Inspect the repository first (inspect_project.py): existing components, tokens, fonts, breakpoints, and conventions win. Reuse the existing component; if it lacks a variant, extend it through its API; if the composition is new, compose existing primitives; only create a new primitive when the gap is real, and put it where the others live. _(covers: reuse → extend → compose → new)_
  - selected for: critical concept (GENERIC): reuse → extend → compose → new
- **Exceptions first: surface what needs attention in lists and tables** `data-exceptions-first` [accessibility/data-display/feedback; heuristic; GENERIC] — Compute the status in the model and show it as a column or badge with a word plus icon plus colour; sort or group exceptions first (or offer a one-tap 'only overdue' filter); show a count in the header/status bar; keep the row otherwise unchanged so scanning stays fast; state the rule that makes an item an exception (e.g. '> 90 days since service'). _(covers: exceptions and anomalies first, no colour alone for status, glanceable status, tabular figures and numeric alignment)_
  - selected for: critical concept (GENERIC): glanceable status
- **Design empty, loading, error, and partial states** `layout-states-empty-loading-error` [states; heuristic; GENERIC] — Empty: what this is, why it is empty, one action. Loading: skeleton with final dimensions, then content; announce completion. Error: what failed, what to do, retry that works; keep entered data. Partial: show what loaded, mark what didn't. On TV, focus must land somewhere valid in each state. _(covers: loading, empty and error states)_
  - selected for: required coverage (GENERIC): loading, empty and error states

Omitted (redundant): comp-product-detail-page (no positive task evidence (screen / subtype / component / job / product / wording) for a core record); comp-data-table (no positive task evidence (screen / subtype / component / job / product / wording) for a core record); comp-data-entry-grid (contamination: product-specific (erp,finance) vs ['iot']; table-specific record with no table evidence in the request)
Filtered out: anti-mobile-desktop-shrunk (platform ['mobile'] not in request ['web']); comp-tv-rail (platform ['tv'] not in request ['web']); comp-list-row-mobile (platform ['mobile', 'tablet'] not in request ['web']); comp-tv-sign-in (platform ['tv'] not in request ['web']); comp-photo-capture-field (platform ['mobile', 'tablet'] not in request ['web']); comp-tv-side-sheet (platform ['tv'] not in request ['web'])

Concept trace (explain): covered state.loading_empty_error, state.offline_sync [CRITICAL DIRECT], process.reuse_first [CRITICAL GENERIC], touch.minimum_target [CRITICAL GENERIC], env.glanceable_status [CRITICAL GENERIC], a11y.live_status, data.refresh_timestamp, state.saving_conflict
  - UNCOVERED interaction.keyboard_navigation: candidates existed but the bundle cap or a lower utility left them out (candidates grid-single-tab-stop, a11y-keyboard-operable, interaction-drag-drop)
  - UNCOVERED interaction.hover_independence: candidates existed but the bundle cap or a lower utility left them out (candidates a11y-keyboard-operable, anti-hover-only-actions, a11y-hover-not-required)
  - UNCOVERED env.outdoor_readability: CRITICAL: carriers filtered before ranking: platform ['kiosk', 'mobile', 'tablet'] not in request ['web']
  - UNCOVERED adaptive.breakpoint_matrix: candidates existed but the bundle cap or a lower utility left them out (candidates web-responsive-breakpoints)
  - UNCOVERED interaction.focus_visible: candidates existed but the bundle cap or a lower utility left them out (candidates grid-single-tab-stop, focus-ring-standard, a11y-focus-visible)
  - UNCOVERED interaction.shortcuts: candidates rejected as contaminated: product-specific (devtools,erp,finance,saas) vs ['iot'] (candidates cta-toolbar-commands)
  - UNCOVERED touch.thumb_reach: candidates rejected as contaminated: product-specific (ecommerce,finance,healthcare,saas) vs ['iot'] (candidates cta-sticky-bar)
