## guidance: Make the dashboard tell the manager which items need reordering today.
status=PARTIAL mode=['refactor'] platform=['web'] input=['keyboard', 'pointer', 'touch'] product=[] screen=['dashboard'] stack=['vue'] density=None env=[] risk=low negatives=[]
budget=moderate mode evidence: refactor: modification of existing UI: make
project context: navigation={'value': 'top-bar', 'status': 'KNOWN', 'evidence': ['top-bar: 8 matches in App.vue, TopBar.vue, tokens.css (shell/layout file)']}, theme={'value': 'unknown', 'status': 'UNKNOWN', 'evidence': ['hex palette: 11 near-white, 6 near-black']}, surfaces={'value': 'bordered-flat', 'status': 'INFERRED', 'evidence': ['borders in 11 files, shadow/elevation in 2']}, radius={'value': 'small', 'status': 'INFERRED', 'evidence': ['most common radius 5 (1×); others [6.0]']}, spacing={'value': 'irregular', 'status': 'UNKNOWN', 'evidence': ['most used spacing values [6, 4, 2]']}, typography={'value': 'humanist-sans', 'status': 'KNOWN', 'evidence': ['font family IBM Plex Sans (2 refs)', 'weights 600, 500', 'encoded type scale in 1 files']}, components={'value': 'unknown', 'status': 'UNKNOWN', 'evidence': []}
concepts required=0.75 covered (6/8); tokens≈989 coverage/1k=6.07 purity=0.75
concerns required=['component', 'structure', 'navigation', 'states', 'interaction', 'accessibility', 'data-display'] covered=0.86 uncovered=['navigation'] recommended=['anti-pattern', 'adaptive'] bundle=7 (core 3 + guardrails 4) diversity=0.86 redundancy=0.46
uncovered required concepts (no record carries them for this context): interaction.hover_independence, interaction.focus_visible

### CORE (what to build)
- **Chart container and interaction** `comp-chart-container` [accessibility/component/data-display/states] — Title that states the question, unit and time range visible, legend as direct labels where possible, tooltip also keyboard-reachable (focusable points or a data table toggle), accessible summary text, consistent palette, responsive (reduce ticks, not data), empty/loading/error states, no animation beyond a single load transition and none on data refresh. Provide the data table or CSV. _(covers: no colour alone for status, chart form chosen from the analytical question, accessible chart summary and table alternative, loading, empty and error states)_
  - selected for: critical concept (SPECIFIC): no colour alone for status, chart form chosen from the analytical question; lexical 0.151, structural 0.58
- **Dashboard grid of modules** `layout-dashboard-grid` [structure] — 12-column responsive grid, modules sized by importance (the primary KPI or chart spans wider), reading order = importance order (top-left first), each module a region with a heading and its own loading/empty/error states. Cards are optional: a divider grid with headings is often clearer than nested boxes.
  - selected for: task evidence: screen, lexical_strong; lexical 0.326, structural 0.64
- **Relationship → scatter / bubble** `chart-relationship` [data-display] — Both axes labelled with units, aspect ratio near 1:1 for correlation reading, bubble area (not radius) encodes size with a size legend, opacity for overplotting, trend line only with the method stated, keyboard-accessible point list or table alternative. _(covers: chart form chosen from the analytical question, accessible chart summary and table alternative)_
  - selected for: task evidence: ; lexical 0.049, structural 0.56

### CRITICAL GUARDRAILS (must hold)
- **Reuse → extend → compose → new (in that order)** `impl-reuse-before-new` [structure; engineering-practice; GENERIC] — Inspect the repository first (inspect_project.py): existing components, tokens, fonts, breakpoints, and conventions win. Reuse the existing component; if it lacks a variant, extend it through its API; if the composition is new, compose existing primitives; only create a new primitive when the gap is real, and put it where the others live. _(covers: reuse → extend → compose → new)_
  - selected for: critical concept (GENERIC): reuse → extend → compose → new
- **Drag and drop: affordance, feedback, keyboard alternative, no layout thrash** `interaction-drag-drop` [interaction; heuristic; GENERIC] — Show a grip or lift affordance; on lift raise the item (shadow/scale) and move it with a transform only; show a snapped drop target with the value it will take (time slot, column); write state once on drop; provide a keyboard/button alternative (arrow keys or a 'Move to…' menu) with focus kept on the moved item; announce the result in a live region; Escape cancels; respect reduced motion. _(covers: discoverable gestures, keyboard navigation and focus order, live region status announcements, no layout shift)_
  - selected for: required coverage (GENERIC): keyboard navigation and focus order, interaction
- **Never colour alone** `a11y-color-not-only` [accessibility/interaction; accessibility-requirement; DIRECT] — Pair colour with text, icon, pattern, weight, or underline. Error fields get an icon and message; chart series get labels or line styles; links in prose get underlines; selected rows get a check or a border, not only a tint. _(covers: no colour alone for status, selected state visible and distinct from focus and hover)_
  - selected for: specialist rule for no colour alone for status (only covered by a composite record)

### OPTIONAL NOTES (apply only when they fit)
- **Exceptions first: surface what needs attention in lists and tables** `data-exceptions-first` [accessibility/data-display/feedback; heuristic; SPECIFIC] — Compute the status in the model and show it as a column or badge with a word plus icon plus colour; sort or group exceptions first (or offer a one-tap 'only overdue' filter); show a count in the header/status bar; keep the row otherwise unchanged so scanning stays fast; state the rule that makes an item an exception (e.g. '> 90 days since service'). _(covers: exceptions and anomalies first, no colour alone for status, glanceable status, tabular figures and numeric alignment)_
  - selected for: recommended coverage (SPECIFIC): exceptions and anomalies first

Omitted (redundant): layout-table-first (no positive task evidence (screen / subtype / component / job / product / wording) for a core record); comp-kpi-tile (no positive task evidence (screen / subtype / component / job / product / wording) for a core record); comp-data-table (no positive task evidence (screen / subtype / component / job / product / wording) for a core record); cta-toolbar-commands (no positive task evidence (screen / subtype / component / job / product / wording) for a core record); cta-contextual-inline (no positive task evidence (screen / subtype / component / job / product / wording) for a core record); nav-top-bar (no positive task evidence (screen / subtype / component / job / product / wording) for a core record)
Filtered out: anti-desktop-scaled-to-tv (platform ['tv'] not in request ['web']); anti-mobile-desktop-shrunk (platform ['mobile'] not in request ['web']); comp-tv-rail (platform ['tv'] not in request ['web']); comp-list-row-mobile (platform ['mobile', 'tablet'] not in request ['web']); dir-social-feed-mobile (platform ['mobile'] not in request ['web']); dir-windows-native-tool (platform ['desktop'] not in request ['web'])

Concept trace (explain): covered state.loading_empty_error, process.reuse_first [CRITICAL GENERIC], interaction.keyboard_navigation, data.chart_by_question [CRITICAL SPECIFIC], a11y.color_not_only [CRITICAL DIRECT], data.accessible_chart_alternative, data.exception_first
  - UNCOVERED interaction.hover_independence: candidates existed but the bundle cap or a lower utility left them out (candidates anti-hover-only-actions, a11y-keyboard-operable, a11y-hover-not-required)
  - UNCOVERED interaction.focus_visible: candidates existed but the bundle cap or a lower utility left them out (candidates grid-single-tab-stop, focus-ring-standard, a11y-focus-visible)
  - UNCOVERED process.safe_modification: candidates existed but the bundle cap or a lower utility left them out (candidates impl-safe-modification)
  - UNCOVERED touch.minimum_target: candidates existed but the bundle cap or a lower utility left them out (candidates media-photo-viewer, a11y-target-size)
  - UNCOVERED adaptive.breakpoint_matrix: candidates existed but the bundle cap or a lower utility left them out (candidates web-responsive-breakpoints)
  - UNCOVERED data.drilldown: candidates existed but the bundle cap or a lower utility left them out (candidates comp-kpi-tile)
  - UNCOVERED data.kpi_comparison: candidates existed but the bundle cap or a lower utility left them out (candidates comp-kpi-tile, chart-compare-bar, chart-progress-gauge)
  - UNCOVERED layout.focal_hierarchy: candidates existed but the bundle cap or a lower utility left them out (candidates metadata-rich, typo-measure-and-rhythm, layout-hierarchy-one-thing)
