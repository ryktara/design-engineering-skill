## guidance: Add a status bar with the last sync time and any failed postings.
status=CONFIDENT mode=['create'] platform=['desktop'] input=['keyboard', 'pointer'] product=['erp'] screen=[] stack=['wpf'] density=high env=[] risk=low negatives=[]
MISSING: brand: no brand assets, guideline, or character description available
budget=moderate mode evidence: create: build/create request
project context: navigation={'value': 'menu-bar', 'status': 'KNOWN', 'evidence': ['menu-bar: 2 matches in App.xaml.cs, PurchaseOrderLinesView.xaml (shell/layout file)']}, theme={'value': 'light-first', 'status': 'KNOWN', 'evidence': ['root/canvas backgrounds: 4 light, 0 dark', 'hex palette: 11 near-white, 1 near-black']}, surfaces={'value': 'bordered-flat', 'status': 'INFERRED', 'evidence': ['borders in 5 files, shadow/elevation in 0']}, radius={'value': 'small', 'status': 'INFERRED', 'evidence': ['most common radius 2 (4×); others [1.0, 10.0]']}, spacing={'value': 4, 'status': 'INFERRED', 'evidence': ['most used spacing values [16, 8, 6, 2, 12]']}, typography={'value': 'humanist-sans', 'status': 'INFERRED', 'evidence': ['font family Segoe UI Variable Text, Segoe UI (1 refs)', 'monospace face Cascadia Mono, Consolas used for a code/number role', 'weights semibold, bold']}, components={'value': 'wpf', 'status': 'KNOWN', 'evidence': ['wpf']}
concepts required=1.0 covered (4/4); tokens≈1063 coverage/1k=3.76 purity=0.66
concerns required=['structure', 'states', 'interaction', 'accessibility', 'data-display'] covered=1.0 uncovered=[] recommended=['navigation', 'anti-pattern', 'brand'] bundle=7 (core 2 + guardrails 5) diversity=0.86 redundancy=0.47

### CORE (what to build)
- **Rich metadata (operational)** `metadata-rich` [content/structure] — Columns with user-controlled visibility and order, consistent formatting per type (dates, currency, IDs in monospace), status as text+colour, truncation with full value on focus/hover and in the detail pane. _(covers: visual hierarchy with one focal point, tabular figures and numeric alignment)_
  - selected for: critical concept (SPECIFIC): tabular figures and numeric alignment; lexical 0.045, structural 0.8
- **Operational workbench** `dir-operational-workbench` [brand/structure] — Character comes from precision: a tight 4 px grid, tabular figures, hairline borders with real contrast, one accent used only for selection and primary commands, quiet surfaces, dense but aligned. Identity via a distinctive neutral tint, a characterful monospace for IDs/values, and a consistent status colour language. No hero, no cards-in-cards, no gradients.
  - selected for: task evidence: product; lexical 0.012, structural 0.8

### CRITICAL GUARDRAILS (must hold)
- **Reuse → extend → compose → new (in that order)** `impl-reuse-before-new` [structure; engineering-practice; GENERIC] — Inspect the repository first (inspect_project.py): existing components, tokens, fonts, breakpoints, and conventions win. Reuse the existing component; if it lacks a variant, extend it through its API; if the composition is new, compose existing primitives; only create a new primitive when the gap is real, and put it where the others live. _(covers: reuse → extend → compose → new)_
  - selected for: critical concept (GENERIC): reuse → extend → compose → new
- **Desktop status bar as the persistent feedback surface, with next-error navigation** `desktop-status-bar-and-error-navigation` [accessibility/feedback/states/structure; platform-standard; SPECIFIC] — One status bar at the bottom of the window with fixed regions (selection summary, sync/save state with timestamp, error count as a link, active filter) separated by real separators, not spaces; validation for the current row/cell is echoed there in words ('Line 50: Quantity must be greater than 0') and the error count opens a list; F8 / Shift+F8 (or the project's convention) walk to the next and previous error and move focus into the cell; error styling never paints over the value text (tint the cell background and keep ≥ 4.5:1 for the text); announce status changes with LiveSetting/UIA so screen readers hear them; the bar keeps its height at every window width. _(covers: inline validation messages and error recovery, live region status announcements, high contrast, keyboard shortcuts / accelerators, persisted workspace and selection)_
  - wpf: StatusBar with StatusBarItem regions and Separator; Validation.ErrorTemplate sets a Border behind the AdornedElementPlaceholder, not over it; RoutedUICommand for NextError (F8).
  - selected for: most relevant record for the request wording
- **Desktop: keyboard is a first-class input** `desktop-keyboard-first` [interaction; platform-standard; SPECIFIC] — Document shortcuts in menus and tooltips; F2 edits, Delete deletes with undo, Ctrl+F finds, F6 cycles panes; grids use arrow keys and Ctrl/Shift selection; every dialog has a default and cancel button; access keys shown on Alt (Windows). _(covers: keyboard navigation and focus order, keyboard shortcuts / accelerators, visible focus)_
  - selected for: required coverage (SPECIFIC): visible focus, keyboard navigation and focus order, interaction
- **Exceptions first: surface what needs attention in lists and tables** `data-exceptions-first` [accessibility/data-display/feedback; heuristic; GENERIC] — Compute the status in the model and show it as a column or badge with a word plus icon plus colour; sort or group exceptions first (or offer a one-tap 'only overdue' filter); show a count in the header/status bar; keep the row otherwise unchanged so scanning stays fast; state the rule that makes an item an exception (e.g. '> 90 days since service'). _(covers: exceptions and anomalies first, no colour alone for status, glanceable status, tabular figures and numeric alignment)_
  - selected for: required coverage (GENERIC): data-display
- **One type scale with named roles** `typo-scale-and-roles` [content/data-display; heuristic; DIRECT] — Roles display/heading/title/body/label/caption/numeric with size, line height, weight, and letter spacing per role; body ≥16 px web/mobile, 14 desktop, 24 TV; numeric role uses tabular lining figures; headings use tighter line height (1.1–1.25) and body 1.4–1.6. Generate with tokens.py scale and map the roles to the framework's text styles. _(covers: tabular figures and numeric alignment, type roles and scale)_
  - selected for: specialist rule for tabular figures and numeric alignment (only covered by a composite record)

Omitted (redundant): dir-windows-native-tool (same category as a core pick (adds no new concept) or incompatible with one); dir-clinical-workstation (no positive task evidence (screen / subtype / component / job / product / wording) for a core record); dir-developer-tool (no positive task evidence (screen / subtype / component / job / product / wording) for a core record); dir-industrial-hmi (no positive task evidence (screen / subtype / component / job / product / wording) for a core record); dir-analytical-console (no positive task evidence (screen / subtype / component / job / product / wording) for a core record); layout-table-first (no positive task evidence (screen / subtype / component / job / product / wording) for a core record)
Filtered out: anti-hero-template (platform ['web'] not in request ['desktop']); comp-media-card (platform ['mobile', 'tv', 'web'] not in request ['desktop']); comp-player-controls (platform ['mobile', 'tv', 'web'] not in request ['desktop']); comp-tv-rail (platform ['tv'] not in request ['desktop']); comp-epg (platform ['tablet', 'tv', 'web'] not in request ['desktop']); comp-tv-sign-in (platform ['tv'] not in request ['desktop'])

Concept trace (explain): covered process.reuse_first [CRITICAL GENERIC], interaction.keyboard_navigation, interaction.focus_visible, table.tabular_figures [CRITICAL DIRECT], interaction.shortcuts, desktop.persist_workspace
  - UNCOVERED state.loading_empty_error: candidates existed but the bundle cap or a lower utility left them out (candidates comp-chart-container, layout-states-empty-loading-error, anti-no-states)
  - UNCOVERED desktop.fluent_materials: candidates existed but the bundle cap or a lower utility left them out (candidates desktop-fluent-materials)
