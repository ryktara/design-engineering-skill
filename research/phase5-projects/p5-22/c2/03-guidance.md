## guidance: Make the Add Habit sheet work when the keyboard is up.
status=CONFIDENT mode=['refactor'] platform=['mobile'] input=['keyboard', 'touch'] product=[] screen=[] stack=['swiftui'] density=None env=[] risk=low negatives=[]
budget=moderate mode evidence: refactor: modification of existing UI: make
project context: navigation={'value': 'bottom-tabs', 'status': 'KNOWN', 'evidence': ['bottom-tabs: 7 matches in README.md, RootTabView.swift, StreaksApp.swift (shell/layout file)']}, theme={'value': 'dual-theme', 'status': 'KNOWN', 'evidence': ['dark theme configuration signals: 2', 'root/canvas backgrounds: 2 light, 2 dark', 'hex palette: 7 near-white, 6 near-black'], 'default': 'light'}, surfaces={'value': 'elevated', 'status': 'INFERRED', 'evidence': ['shadow/elevation in 5 files, borders in 1']}, radius={'value': 'small', 'status': 'INFERRED', 'evidence': ['most common radius 5 (4×); others [3.0, 8.0]']}, spacing={'value': 'irregular', 'status': 'UNKNOWN', 'evidence': ['most used spacing values [6, 32, 2, 3, 4]']}, typography={'value': 'custom', 'status': 'KNOWN', 'evidence': ['font family System (7 refs)', 'weights 600, 500, 700, 800', 'encoded type scale in 4 files']}, components={'value': 'unknown', 'status': 'UNKNOWN', 'evidence': []}
concepts required=1.0 covered (7/7); tokens≈1289 coverage/1k=5.43 purity=0.84
concerns required=['component', 'structure', 'interaction', 'accessibility'] covered=1.0 uncovered=[] recommended=['navigation', 'states', 'anti-pattern', 'adaptive', 'feedback'] bundle=8 (core 3 + guardrails 5) diversity=0.88 redundancy=0.59

### Core (what to build)
- **Drawer / side panel** `comp-drawer-panel` [component/interaction/navigation] — Inline (pushes content) on wide screens, overlay on narrow; width from tokens (320–480 px); heading + close; focus moves in on open and returns on close; content scrolls independently; TV: side sheet that keeps the player/content visible and traps DPAD inside until BACK. _(covers: drawer / side panel focus in and out)_
  - swiftui: NavigationSplitView inspector / .inspector(isPresented:); tvOS: custom side sheet with .focusSection().
  - selected for: highest-scoring component with lexical evidence; lexical 0.124, structural 0.52
- **Dialog / modal** `comp-dialog` [accessibility/component/feedback/interaction] — Title as heading, one primary action, safe default for destructive confirmations, focus management per dialog rule, sized to content with max width, scroll inside the body not the page, Escape and close button, backdrop click closes only for non-destructive dialogs. Mobile: bottom sheet or full-screen; TV: full-screen with first focus on the safe action. _(covers: dialog focus management, confirmation of destructive or high-risk actions)_
  - swiftui: .alert / .confirmationDialog / .sheet; tvOS uses full-screen presentation.
  - selected for: highest-scoring component with lexical evidence; lexical 0.131, structural 0.4
- **Full-size photo viewer** `media-photo-viewer` [component] — Thumbnails at least 2-up and decoded at display size × DPR; tap opens a full-screen viewer route (system back closes it) with pinch/double-tap zoom plus an explicit zoom button, labelled previous/next, retake/remove in the bar, dark chrome with high-contrast controls, and 48 dp targets; keep the report state when returning. _(covers: image sizing and formats, discoverable gestures, BACK behaviour, large touch targets (≥44–48 px))_
  - selected for: required coverage: large touch targets (≥44–48 px); lexical 0.02, structural 0.58

### Guardrails (must hold)
- **Mobile: keyboard and input types** `mobile-keyboard-ime` [component/feedback/interaction; platform-standard] — Set keyboard type and autocomplete/textContentType/autofillHints per field, return key action (Next/Done), scroll the focused field above the keyboard, keep the primary action reachable while the keyboard is open (or on the keyboard toolbar), and dismiss on tap outside for non-modal forms. _(covers: on-screen keyboard (IME) aware layout)_
  - selected for: most relevant record for the request wording
- **Reuse → extend → compose → new (in that order)** `impl-reuse-before-new` [structure; engineering-practice] — Inspect the repository first (inspect_project.py): existing components, tokens, fonts, breakpoints, and conventions win. Reuse the existing component; if it lacks a variant, extend it through its API; if the composition is new, compose existing primitives; only create a new primitive when the gap is real, and put it where the others live. _(covers: reuse → extend → compose → new)_
  - selected for: required coverage: structure, reuse → extend → compose → new
- **Drag and drop: affordance, feedback, keyboard alternative, no layout thrash** `interaction-drag-drop` [interaction; heuristic] — Show a grip or lift affordance; on lift raise the item (shadow/scale) and move it with a transform only; show a snapped drop target with the value it will take (time slot, column); write state once on drop; provide a keyboard/button alternative (arrow keys or a 'Move to…' menu) with focus kept on the moved item; announce the result in a live region; Escape cancels; respect reduced motion. _(covers: discoverable gestures, keyboard navigation and focus order, live region status announcements, no layout shift)_
  - selected for: required coverage: keyboard navigation and focus order
- **Dialog focus management** `a11y-modal-dialog` [accessibility/interaction; accessibility-requirement] — On open: focus the first meaningful control (or the heading), trap Tab inside, inert the background, label the dialog by its title. On close: return focus to the invoker. Escape and the visible close button both close; destructive confirmations put the safe action as default. Use <dialog>/showModal, ContentDialog, .sheet, ModalBottomSheet rather than a div overlay. On TV the sheet opens with focus on the current value and BACK closes it and returns focus to the opener; on kiosks the dialog is full-width with the primary action within reach. _(covers: dialog focus management, focus restoration)_
  - selected for: required coverage: focus restoration
- **Every interactive colour has hover/pressed/focus/disabled/selected** `color-states-complete` [brand/interaction; heuristic] — Define state tokens per role (action.primary-hover/-pressed, bg.selected, text.disabled), keep label contrast on every state, make disabled visibly weaker but readable (≥3:1 recommended even though exempt), selected ≠ focused ≠ hovered. Dark theme redefines all of them. _(covers: visible focus, selected state visible and distinct from focus and hover)_
  - selected for: required coverage: visible focus

Omitted (redundant): media-resume-and-details (contamination: product-specific with product unknown; low purity 0.17: most of its concepts are off-task); comp-product-detail-page (contamination: product-specific with product unknown; low purity 0.20: most of its concepts are off-task); comp-filters (contamination: product-specific with product unknown; low purity 0.00: most of its concepts are off-task; search-specific record with no search evidence in the request); cta-sticky-bar (visual pattern without wording evidence on an existing UI (would restyle what the task does not concern))
Filtered out: anti-desktop-scaled-to-tv (platform ['tv'] not in request ['mobile']); anti-default-fonts (platform ['web'] not in request ['mobile']); anti-hover-only-actions (platform ['desktop', 'web'] not in request ['mobile']); chart-relationship (platform ['desktop', 'web'] not in request ['mobile']); chart-heatmap-matrix (platform ['desktop', 'web'] not in request ['mobile']); comp-data-table (platform ['desktop', 'web'] not in request ['mobile'])

Concept trace (explain): covered touch.ime_keyboard, a11y.dialog_focus, process.reuse_first, touch.minimum_target, interaction.keyboard_navigation, interaction.focus_visible, interaction.focus_restore, touch.gestures_discoverable, a11y.live_status, feedback.confirmation_destructive
  - UNCOVERED process.safe_modification: candidates existed but the bundle cap or a lower utility left them out (candidates impl-safe-modification)
  - UNCOVERED touch.safe_areas: candidates existed but the bundle cap or a lower utility left them out (candidates cta-sticky-bar, mobile-safe-areas)
  - UNCOVERED touch.thumb_reach: candidates existed but the bundle cap or a lower utility left them out (candidates mobile-thumb-reach, cta-sticky-bar, anti-mobile-desktop-shrunk)
  - UNCOVERED navigation.platform_grammar: candidates existed but the bundle cap or a lower utility left them out (candidates mobile-platform-navigation, nav-bottom-tabs)
  - UNCOVERED interaction.shortcuts: carriers filtered before ranking: platform ['desktop', 'web'] not in request ['mobile']
