## guidance: Checkout on phones: the order summary pushes the pay button below the fold.
status=AMBIGUOUS mode=['responsive', 'audit', 'refactor'] platform=['mobile'] input=['touch'] product=['ecommerce'] screen=['checkout'] stack=['html-css'] density=None env=[] risk=medium negatives=[]
CONFLICTS: platform (request kept; repository platform recorded as context)
budget=moderate mode evidence: responsive: explicit: on phones | audit: layout/structure defect on existing UI | refactor: structural fix follows
project context: navigation={'value': 'top-bar', 'status': 'KNOWN', 'evidence': ['top-bar: 4 matches in checkout.html, index.html, product.html (shell/layout file)']}, theme={'value': 'light-first', 'status': 'INFERRED', 'evidence': ['hex palette: 6 near-white, 2 near-black']}, surfaces={'value': 'elevated', 'status': 'INFERRED', 'evidence': ['weak signal: shadow 1, border 1']}, radius={'value': 'pill', 'status': 'INFERRED', 'evidence': ['most common radius 999 (5×); others [5.0, 4.0]']}, spacing={'value': 'irregular', 'status': 'UNKNOWN', 'evidence': ['most used spacing values [4, 14, 2]']}, typography={'value': 'geometric-sans', 'status': 'INFERRED', 'evidence': ['font family Inter (1 refs)', 'weights 600, 500, 400', 'encoded type scale in 1 files']}, components={'value': 'unknown', 'status': 'UNKNOWN', 'evidence': []}
concepts required=0.86 covered (6/7); tokens≈878 coverage/1k=6.83 purity=0.97
concerns required=['adaptive', 'structure', 'interaction', 'accessibility', 'feedback'] covered=1.0 uncovered=[] recommended=['navigation'] bundle=6 (core 2 + guardrails 4) diversity=1.0 redundancy=0.33
uncovered required concepts (no record carries them for this context): adaptive.breakpoint_matrix

### Core (what to build)
- **One-page checkout** `comp-checkout-one-page` [component/feedback/states/structure] — Order summary is visible on desktop (side column) and collapsible-but-present at the top on phones with the total always shown; guest checkout first, account optional; sections in the order contact → shipping → payment → review, each with a visible heading and inline validation on blur plus a focused error summary on submit; address fields use autocomplete attributes and correct input types/IME; the pay button states the amount, is disabled only while processing (with a visible status), and is protected against double submission; trust and cost information (shipping, tax) appears before payment, never as a surprise; progress is saved locally so a reload does not lose entries. _(covers: inline validation messages and error recovery, on-screen keyboard (IME) aware layout, confirmation of destructive or high-risk actions, saving, saved and conflict states, one primary action per view, trust and cost transparency before commitment)_
  - html-css: autocomplete="shipping postal-code" etc.; inputmode="numeric" for card and postal fields; aria-describedby error links; aria-live status on the pay button.
  - selected for: highest-scoring component with lexical evidence; lexical 0.791, structural 0.7
- **One primary action per screen** `cta-single-primary` [interaction/structure] — Exactly one filled button per view, secondaries as outlined/text, destructive actions separated and confirmed, button label is a verb phrase naming the outcome ('Save changes'), disabled only with an explanation nearby. _(covers: one primary action per view)_
  - selected for: highest-scoring pattern with lexical evidence; lexical 0.114, structural 0.44

### Guardrails (must hold)
- **Target size by platform** `a11y-target-size` [accessibility/interaction; accessibility-requirement] — Web: ≥24×24 CSS px minimum (WCAG 2.5.8), 44×44 recommended for touch. iOS ≥44 pt, Android ≥48 dp, kiosk ≥60 px, desktop pointer ≥24 epx with 4–8 px spacing. Extend the hit area beyond the visual glyph rather than enlarging the icon. _(covers: large touch targets (≥44–48 px))_
  - selected for: required coverage: accessibility, large touch targets (≥44–48 px)
- **Design empty, loading, error, and partial states** `layout-states-empty-loading-error` [states; heuristic] — Empty: what this is, why it is empty, one action. Loading: skeleton with final dimensions, then content; announce completion. Error: what failed, what to do, retry that works; keep entered data. Partial: show what loaded, mark what didn't. On TV, focus must land somewhere valid in each state. _(covers: loading, empty and error states)_
  - selected for: required coverage: loading, empty and error states
- **Mobile: safe areas and system insets** `mobile-safe-areas` [adaptive/structure; platform-standard] — Content respects safe-area insets (SwiftUI safeAreaInset / .ignoresSafeArea only for backgrounds, Compose WindowInsets + edge-to-edge, RN SafeAreaView/useSafeAreaInsets, web env(safe-area-inset-*)); bottom actions sit above the home indicator/gesture bar; keyboard (IME) insets push the focused field into view. _(covers: safe areas and notches)_
  - selected for: required coverage: adaptive
- **Mobile: follow the platform navigation grammar** `mobile-platform-navigation` [navigation; platform-standard] — iOS: tab bar + navigation stack with large titles where idiomatic, sheets for secondary tasks, swipe back. Android: navigation bar, predictive back, top app bar, modal bottom sheets, up vs back. Cross-platform frameworks still map to these; state deviations as brand decisions. _(covers: platform navigation grammar)_
  - selected for: recommended coverage: navigation, platform navigation grammar

Omitted (redundant): comp-wizard-stepper (contamination: low purity 0.00: most of its concepts are off-task; wizard-specific record with no wizard evidence in the request); comp-form (same category as a core pick (adds no new concept) or incompatible with one); nav-wizard (same category as a core pick (adds no new concept) or incompatible with one)
Filtered out: anti-hero-template (platform ['web'] not in request ['mobile']); anti-hover-only-actions (platform ['desktop', 'web'] not in request ['mobile']); comp-data-table (platform ['desktop', 'web'] not in request ['mobile']); comp-data-entry-grid (platform ['desktop', 'web'] not in request ['mobile']); comp-tv-sign-in (platform ['tv'] not in request ['mobile']); nav-top-bar (platform ['web'] not in request ['mobile'])
