## guidance: Stats screen in dark mode: the bars are barely visible.
status=CONFIDENT mode=['polish', 'audit'] platform=['mobile'] input=['touch'] product=['ecommerce'] screen=[] stack=['swiftui'] density=None env=[] risk=low negatives=[]
MISSING: brand: no brand assets, guideline, or character description available
budget=low mode evidence: polish: visual defect on existing UI | audit: diagnose first
project context: navigation={'value': 'bottom-tabs', 'status': 'KNOWN', 'evidence': ['bottom-tabs: 7 matches in README.md, RootTabView.swift, StreaksApp.swift (shell/layout file)']}, theme={'value': 'dual-theme', 'status': 'INFERRED', 'evidence': ['dark theme configuration signals: 2', 'hex palette: 0 near-white, 0 near-black']}, surfaces={'value': 'elevated', 'status': 'INFERRED', 'evidence': ['shadow/elevation in 4 files, borders in 0']}, radius={'value': 'medium', 'status': 'INFERRED', 'evidence': ['most common radius 8 (1×); others []']}, spacing={'value': 4, 'status': 'INFERRED', 'evidence': ['most used spacing values [2, 4, 12, 16]']}, typography={'value': 'humanist-sans', 'status': 'INFERRED', 'evidence': ['font family Nunito (1 refs)', 'encoded type scale in 3 files']}, components={'value': 'unknown', 'status': 'UNKNOWN', 'evidence': []}
concepts required=1.0 covered (4/4); tokens≈952 coverage/1k=4.2 purity=0.94
concerns required=['structure', 'anti-pattern', 'interaction', 'accessibility'] covered=1.0 uncovered=[] recommended=['content', 'component', 'adaptive'] bundle=8 (core 2 + guardrails 6) diversity=1.0 redundancy=0.46

### Core (what to build)
- **Sticky action bar** `cta-sticky-bar` [interaction/structure] — Bottom-fixed on mobile inside the safe area, sticky footer on desktop; content gets bottom padding equal to the bar height; the bar must not obscure a focused field (WCAG 2.4.11) so scroll the field into view above it.
  - selected for: highest-scoring pattern with lexical evidence; lexical 0.154, structural 0.56
- **Bottom tab bar** `nav-bottom-tabs` [navigation] — 3–5 items, icon + label always (no icon-only), safe-area aware, current item indicated by more than tint. Each tab keeps its own navigation stack. Don't put actions (compose, add) in the tab bar unless it is the app's primary action and it is styled as an action, not a destination. _(covers: platform navigation grammar)_
  - swiftui: TabView with Label(text, systemImage); avoid custom bars unless brand requires, since the system bar handles Dynamic Type and VoiceOver.
  - selected for: highest-scoring pattern with lexical evidence; lexical 0.122, structural 0.42

### Guardrails (must hold)
- **Dark mode by inversion** `anti-dark-mode-inversion` [anti-pattern/brand; platform-standard] — Redesign surfaces (elevation = lighter), desaturate accents, use 87/60/38% white text steps, re-validate every pair, define dark chart palettes. See the dark mode rule. _(covers: dark mode as a surface redesign, not inversion)_
  - selected for: most relevant record for the request wording
- **Dark mode is a redesign of surfaces, not an inversion** `color-dark-mode-rules` [accessibility/brand; platform-standard] — Elevation = lighter surface (not darker shadow); desaturate accents; text 87/60/38% white steps for primary/secondary/disabled; borders lighten; images may need a slight dim; charts get a dark palette; re-validate every contrast pair. _(covers: dark mode as a surface redesign, not inversion)_
  - selected for: most relevant record for the request wording
- **Measure, rhythm, and hierarchy by contrast of size and weight** `typo-measure-and-rhythm` [content/structure; heuristic] — 45–75 characters per line for prose; vertical spacing from the spacing scale tied to line height; hierarchy from clear jumps (≥1.25×) in size or weight, not from five near-identical sizes; headings closer to the content below than to the content above. _(covers: readable line length, visual hierarchy with one focal point)_
  - selected for: required coverage: visual hierarchy with one focal point
- **Spacing from one scale, grouping by proximity** `layout-spacing-scale` [structure; heuristic] — A geometric-ish scale (4/8/12/16/24/32/48/64), inside-group spacing smaller than between-group spacing (ratio ≥1.5×), alignment to a grid, consistent inset per container type, optical alignment for icons and text baselines. Inconsistent spacing is the most common 'unprofessional' signal. _(covers: consistent spacing scale)_
  - selected for: required coverage: consistent spacing scale
- **Target size by platform** `a11y-target-size` [accessibility/interaction; accessibility-requirement] — Web: ≥24×24 CSS px minimum (WCAG 2.5.8), 44×44 recommended for touch. iOS ≥44 pt, Android ≥48 dp, kiosk ≥60 px, desktop pointer ≥24 epx with 4–8 px spacing. Extend the hit area beyond the visual glyph rather than enlarging the icon. _(covers: large touch targets (≥44–48 px))_
  - selected for: required coverage: large touch targets (≥44–48 px)
- **Reuse → extend → compose → new (in that order)** `impl-reuse-before-new` [structure; engineering-practice] — Inspect the repository first (inspect_project.py): existing components, tokens, fonts, breakpoints, and conventions win. Reuse the existing component; if it lacks a variant, extend it through its API; if the composition is new, compose existing primitives; only create a new primitive when the gap is real, and put it where the others live. _(covers: reuse → extend → compose → new)_
  - selected for: required coverage: reuse → extend → compose → new

Omitted (redundant): comp-kpi-tile (contamination: product-specific (erp,finance,healthcare,iot,saas) vs ['ecommerce']; low purity 0.00: most of its concepts are off-task; chart-specific record with no chart evidence in the request); chart-compare-bar (contamination: low purity 0.00: most of its concepts are off-task; chart record outside a data-visualisation task); chart-composition (contamination: low purity 0.00: most of its concepts are off-task; chart record outside a data-visualisation task); color-dark-accent (contamination: product-specific (devtools,finance,iot,media) vs ['ecommerce'])
Filtered out: anti-desktop-scaled-to-tv (platform ['tv'] not in request ['mobile']); anti-hover-only-actions (platform ['desktop', 'web'] not in request ['mobile']); chart-heatmap-matrix (platform ['desktop', 'web'] not in request ['mobile']); comp-data-table (platform ['desktop', 'web'] not in request ['mobile']); comp-command-palette (platform ['desktop', 'web'] not in request ['mobile']); comp-data-entry-grid (platform ['desktop', 'web'] not in request ['mobile'])
