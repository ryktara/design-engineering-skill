## guidance: Add a Spanish language switch to the pick-up flow.
status=CONFIDENT mode=['create'] platform=['kiosk'] input=['touch'] product=['healthcare', 'iot'] screen=[] stack=['html-css'] density=None env=['public'] risk=medium negatives=[]
MISSING: brand: no brand assets, guideline, or character description available
budget=moderate mode evidence: create: build/create request on an existing surface
project context: navigation={'value': 'hub-spoke', 'status': 'KNOWN', 'evidence': ['hub-spoke: 29 matches in README.md, app.js, i18n.js (shell/layout file)']}, theme={'value': 'unknown', 'status': 'UNKNOWN', 'evidence': ['hex palette: 25 near-white, 17 near-black']}, surfaces={'value': 'elevated', 'status': 'INFERRED', 'evidence': ['weak signal: shadow 1, border 1']}, radius={'value': 'medium', 'status': 'INFERRED', 'evidence': ['most common radius 12 (1×); others [20.0, 32.0]']}, spacing={'value': 'irregular', 'status': 'UNKNOWN', 'evidence': ['most used spacing values [6, 16]']}, typography={'value': 'humanist-sans', 'status': 'KNOWN', 'evidence': ['font family Nunito (2 refs)', 'weights 700, 800, 600, 500', 'tabular numerals']}, components={'value': 'unknown', 'status': 'UNKNOWN', 'evidence': []}
concepts required=1.0 covered (6/6); tokens≈939 coverage/1k=6.39 purity=0.83
concerns required=['structure', 'states', 'interaction', 'accessibility', 'environment', 'privacy'] covered=1.0 uncovered=[] recommended=['navigation', 'anti-pattern', 'brand', 'feedback', 'performance'] bundle=6 (core 2 + guardrails 4) diversity=1.0 redundancy=0.25

### Core (what to build)
- **Industrial HMI / control panel** `dir-industrial-hmi` [brand/structure] — Grey-scale by default so alarms are the only saturated colour (ISA-101 high-performance HMI), large touch targets with gloves in mind, state shown by shape + text + colour, trend sparklines with thresholds, confirm-and-hold for hazardous commands, no decorative anything. Identity via the schematic style and status semantics.
  - selected for: no record matched the wording; best structural fit for platform/product; lexical 0.0, structural 0.72
- **On-screen keypad / keyboard for kiosks** `comp-kiosk-keypad` [accessibility/component/feedback/interaction/privacy] — Keys ≥ 64 px with ≥ 16 px gaps, one key = one accessible name ('Hyphen', 'Delete', 'Space'), the keypad is a role=group labelled by the field it edits; the value box is focusable and read back through a polite live region as it changes; a persistent format hint (not a placeholder) is tied to the field with aria-describedby and errors set aria-invalid with a specific message ('Day must be 1–31'); focus never falls to the page body after a tap (re-focus the equivalent key after a re-render); a physical keypad or scanner wedge writes into the same field (human-speed keys fill, Enter continues, bursts are treated as scans); masked entry for PINs with a visible dot per character; in accessible mode the keypad and value sit in the reach zone (lower part of the screen). _(covers: large touch targets (≥44–48 px), accessible names and labels, live region status announcements, inline validation messages and error recovery, focus restoration, masking of sensitive values with explicit reveal)_
  - html-css: <div role="group" aria-labelledby=…> of <button> keys; <output aria-live="polite"> for the value; keydown handler distinguishes scanner bursts by inter-key interval.
  - selected for: required coverage: accessibility, masking of sensitive values with explicit reveal; lexical 0.0, structural 0.72

### Guardrails (must hold)
- **Internationalisation: expansion, RTL, formats** `i18n-text-expansion-rtl` [adaptive/content; engineering-practice] — Allow 30–50% text expansion (German, Finnish), test with pseudo-localisation, use logical CSS properties / start-end alignment, mirror navigation and progress in RTL, format dates/numbers/currency by locale, verify font coverage per script, keep tabular figures across locales. A language switch is one visible control on every screen of the flow, labelled in its own language (Español, not 'Spanish'); switching keeps the session, typed input, selection and focus, announces the change politely, and re-renders every label including error text. _(covers: text expansion and RTL)_
  - selected for: most relevant record for the request wording
- **Kiosk: public, hurried, standing users** `kiosk-public-use` [environment/interaction/privacy; platform-standard] — Targets ≥60 px, body text ≥20 px, high contrast for glare, one task per screen, reachable-height controls (ADA reach ranges: interactive elements within 380–1220 mm), idle timeout with countdown that clears the session, attract screen as the hub, audio/visual feedback on every tap, and a visible way to cancel at every step. _(covers: large touch targets (≥44–48 px), privacy of on-screen data on shared devices, session expiry and idle reset)_
  - selected for: required coverage: environment, interaction, privacy, privacy of on-screen data on shared devices, session expiry and idle reset, large touch targets (≥44–48 px)
- **Reuse → extend → compose → new (in that order)** `impl-reuse-before-new` [structure; engineering-practice] — Inspect the repository first (inspect_project.py): existing components, tokens, fonts, breakpoints, and conventions win. Reuse the existing component; if it lacks a variant, extend it through its API; if the composition is new, compose existing primitives; only create a new primitive when the gap is real, and put it where the others live. _(covers: reuse → extend → compose → new)_
  - selected for: required coverage: reuse → extend → compose → new
- **Auto-rotating and timed content is controllable** `a11y-time-and-auto` [accessibility/motion/states; accessibility-requirement] — Pause/stop control, stop on focus/hover, keyboard next/previous, announce slide position; timeouts warn and allow extension; kiosks show a visible countdown before resetting. _(covers: session expiry and idle reset, reduced motion)_
  - selected for: required coverage: states
Filtered out: chart-flow-sankey (platform ['desktop', 'web'] not in request ['kiosk']); comp-data-entry-grid (platform ['desktop', 'web'] not in request ['kiosk']); comp-product-detail-page (platform ['mobile', 'web'] not in request ['kiosk']); comp-checkout-one-page (platform ['mobile', 'web'] not in request ['kiosk']); media-resume-and-details (platform ['mobile', 'tv', 'web'] not in request ['kiosk']); comp-photo-capture-field (platform ['mobile', 'tablet'] not in request ['kiosk'])

Concept trace (explain): covered content.i18n_expansion, process.reuse_first, touch.minimum_target, privacy.shared_device, state.session_expiry, privacy.sensitive_masking, a11y.live_status
  - UNCOVERED state.loading_empty_error: candidates existed but the bundle cap or a lower utility left them out (candidates anti-no-states, layout-states-empty-loading-error, comp-empty-state)
  - UNCOVERED touch.gestures_discoverable: carriers were not admissible for this platform/input/environment
  - UNCOVERED touch.thumb_reach: carriers filtered before ranking: platform ['mobile', 'tablet', 'web'] not in request ['kiosk']
  - UNCOVERED env.glanceable_status: candidates existed but the bundle cap or a lower utility left them out (candidates data-exceptions-first)
  - UNCOVERED feedback.confirmation_destructive: candidates existed but the bundle cap or a lower utility left them out (candidates comp-dialog, comp-settings-screen)
