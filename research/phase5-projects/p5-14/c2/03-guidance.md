## guidance: Purchase-order lines: operators keep typing the quantity into the price column.
status=CONFIDENT mode=['audit', 'refactor'] platform=['desktop'] input=['keyboard', 'pointer'] product=['erp'] screen=[] stack=['wpf'] density=high env=[] risk=low negatives=[]
budget=moderate mode evidence: audit: interaction defect on existing UI | refactor: fix follows the diagnosis
project context: navigation={'value': 'menu-bar', 'status': 'KNOWN', 'evidence': ['menu-bar: 2 matches in App.xaml.cs, PurchaseOrderLinesView.xaml (shell/layout file)']}, theme={'value': 'light-first', 'status': 'KNOWN', 'evidence': ['root/canvas backgrounds: 4 light, 0 dark', 'hex palette: 11 near-white, 1 near-black']}, surfaces={'value': 'bordered-flat', 'status': 'INFERRED', 'evidence': ['borders in 5 files, shadow/elevation in 0']}, radius={'value': 'small', 'status': 'INFERRED', 'evidence': ['most common radius 2 (4×); others [1.0, 10.0]']}, spacing={'value': 4, 'status': 'INFERRED', 'evidence': ['most used spacing values [16, 8, 6, 2, 12]']}, typography={'value': 'humanist-sans', 'status': 'INFERRED', 'evidence': ['font family Segoe UI Variable Text, Segoe UI (1 refs)', 'monospace face Cascadia Mono, Consolas used for a code/number role', 'weights semibold, bold']}, components={'value': 'wpf', 'status': 'KNOWN', 'evidence': ['wpf']}
concepts required=1.0 covered (5/5); tokens≈1326 coverage/1k=3.77 purity=0.81
concerns required=['accessibility', 'interaction', 'component', 'data-display'] covered=1.0 uncovered=[] recommended=['states', 'anti-pattern', 'feedback'] bundle=6 (core 1 + guardrails 5) diversity=0.67 redundancy=0.4

### Core (what to build)
- **Data table / grid** `comp-data-table` [component/data-display/structure] — Sticky header, row height by density token, zebra striping optional (prefer hover/selection highlight), column resize/reorder/visibility persisted, sort indicator with aria-sort, selection checkbox column with header select-all and a count, row actions visible on focus as well as hover, inline edit with Enter/Escape, keyboard grid navigation (arrows, Home/End, PageUp/Down), virtualised rows, loading skeleton rows, empty state inside the table body. Financial tables: see numeric rule. _(covers: selection state and bulk actions, inline editing, virtualization of long collections, tabular figures and numeric alignment, pagination / load-more strategy, selected state visible and distinct from focus and hover)_
  - wpf: DataGrid with virtualisation flags; DevExpress GridControl if present in the project.
  - selected for: highest-scoring component with lexical evidence; lexical 0.152, structural 0.8

### Guardrails (must hold)
- **Editable grids: make the current column and its unit unmistakable** `table-column-disambiguation` [data-display; heuristic] — Mark the active column in the header (bar + strong text); show the unit as an affix inside the editor (EA suffix for quantities, currency prefix for money); use role-distinct formats (integers for counts, fixed decimals for money, unit in the header); bracket money columns with a stronger divider; validate implausible values (a price typed as a quantity) inline before commit. _(covers: tabular figures and numeric alignment, inline editing, inline validation messages and error recovery)_
  - selected for: most relevant record for the request wording
- **Grids with row actions are one Tab stop** `grid-single-tab-stop` [accessibility/data-display/interaction; accessibility-requirement] — Tab enters the grid once and leaves it once; arrow keys move between cells/rows (roving tabindex or a focus manager), Enter/Space activates the focused cell's action, Escape returns from an edited cell to navigation mode; row actions become reachable when the row or actions cell is focused and are also available from a row context/actions menu; a table with 24 rows must never produce 24 Tab stops per action column. Announce the current row/column (aria-rowindex/colindex or the platform's automation properties) and keep a visible focus indicator on the active cell. _(covers: keyboard navigation and focus order, visible focus, selection state and bulk actions, selected state visible and distinct from focus and hover)_
  - wpf: KeyboardNavigation.TabNavigation="Once" on the DataGrid; use DataGridTemplateColumn with Focusable="False" on the row buttons and a command on the row.
  - selected for: required coverage: accessibility, interaction, visible focus, keyboard navigation and focus order
- **Desktop status bar as the persistent feedback surface, with next-error navigation** `desktop-status-bar-and-error-navigation` [accessibility/feedback/states/structure; platform-standard] — One status bar at the bottom of the window with fixed regions (selection summary, sync/save state with timestamp, error count as a link, active filter) separated by real separators, not spaces; validation for the current row/cell is echoed there in words ('Line 50: Quantity must be greater than 0') and the error count opens a list; F8 / Shift+F8 (or the project's convention) walk to the next and previous error and move focus into the cell; error styling never paints over the value text (tint the cell background and keep ≥ 4.5:1 for the text); announce status changes with LiveSetting/UIA so screen readers hear them; the bar keeps its height at every window width. _(covers: inline validation messages and error recovery, live region status announcements, high contrast, keyboard shortcuts / accelerators, persisted workspace and selection)_
  - wpf: StatusBar with StatusBarItem regions and Separator; Validation.ErrorTemplate sets a Border behind the AdornedElementPlaceholder, not over it; RoutedUICommand for NextError (F8).
  - selected for: recommended coverage: feedback, states, high contrast, live region status announcements, persisted workspace and selection, keyboard shortcuts / accelerators
- **Only the happy state was designed** `anti-no-states` [anti-pattern/states; heuristic] — Enumerate states per screen and per interactive component before implementation and verify each visually; test with long strings, zero items, 10k items, and slow networks. _(covers: loading, empty and error states)_
  - selected for: recommended coverage: anti-pattern, loading, empty and error states
- **Windows: Fluent layering and materials, sparingly** `desktop-fluent-materials` [brand/structure; platform-standard] — Use the system resources (SystemControl*, Layer/Card brushes, ControlCornerRadius) so light/dark/high-contrast themes work; Mica for the window, Acrylic only for transient surfaces; verify high-contrast mode renders every state. _(covers: Fluent system resources and materials)_
  - selected for: recommended coverage: Fluent system resources and materials
Filtered out: anti-hero-template (platform ['web'] not in request ['desktop']); anti-scroll-animation-everything (platform ['mobile', 'web'] not in request ['desktop']); comp-media-card (platform ['mobile', 'tv', 'web'] not in request ['desktop']); comp-epg (platform ['tablet', 'tv', 'web'] not in request ['desktop']); comp-list-row-mobile (platform ['mobile', 'tablet'] not in request ['desktop']); comp-product-detail-page (platform ['mobile', 'web'] not in request ['desktop'])

Concept trace (explain): covered table.inline_edit, table.tabular_figures, interaction.keyboard_navigation, interaction.focus_visible, feedback.validation_errors, a11y.contrast, state.loading_empty_error, interaction.shortcuts, desktop.persist_workspace, desktop.fluent_materials, a11y.live_status
  - UNCOVERED a11y.accessible_names: candidates existed but the bundle cap or a lower utility left them out (candidates a11y-native-semantics, a11y-labels-names)
  - UNCOVERED process.reuse_first: candidates existed but the bundle cap or a lower utility left them out (candidates impl-reuse-before-new)
  - UNCOVERED state.saving_conflict: candidates existed but the bundle cap or a lower utility left them out (candidates states-persistence-and-session)
  - UNCOVERED form.autofill_attributes: candidates existed but the bundle cap or a lower utility left them out (candidates comp-form)
