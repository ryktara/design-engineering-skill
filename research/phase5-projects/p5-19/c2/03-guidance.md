## guidance: Sync status is a spinner that never says what is happening.
status=CONFIDENT mode=['audit', 'refactor'] platform=['mobile'] input=['touch'] product=['iot'] screen=[] stack=['flutter'] density=None env=['gloves', 'low-bandwidth', 'outdoor'] risk=low negatives=[]
budget=low mode evidence: audit: perceived-performance defect | refactor: fix follows the diagnosis
project context: navigation={'value': 'top-bar', 'status': 'KNOWN', 'evidence': ['top-bar: 6 matches in checklist_screen.dart, defect_report_screen.dart, settings_screen.dart (shell/layout file)']}, theme={'value': 'dual-theme', 'status': 'KNOWN', 'evidence': ['dark theme configuration signals: 5', 'light theme configuration signals: 2', 'hex palette: 13 near-white, 6 near-black'], 'default': 'light'}, surfaces={'value': 'flat-tonal', 'status': 'UNKNOWN', 'evidence': ['no shadow or border declarations found']}, radius={'value': 'small', 'status': 'INFERRED', 'evidence': ['most common radius 4 (1×); others []']}, spacing={'value': 'irregular', 'status': 'UNKNOWN', 'evidence': ['most used spacing values [2, 12]']}, typography={'value': 'custom', 'status': 'KNOWN', 'evidence': ['font family System (7 refs)', 'weights w600, w700, w400', 'encoded type scale in 2 files']}, components={'value': 'go_router, riverpod', 'status': 'KNOWN', 'evidence': ['go_router', 'riverpod']}
concepts required=1.0 covered (6/6); tokens≈879 coverage/1k=6.83 purity=1.0
concerns required=['accessibility', 'interaction', 'component', 'environment', 'states'] covered=1.0 uncovered=[] recommended=['anti-pattern', 'feedback', 'adaptive', 'performance'] bundle=6 (core 1 + guardrails 5) diversity=1.0 redundancy=0.4

### Core (what to build)
- **Service app (mobile hub-and-spoke)** `dir-service-app-mobile` [brand/structure] — Home as a task hub with large labelled entries and the account balance/status as the single focal element, spokes as linear flows with one primary action each, list rows for history, system type styles for Dynamic Type/font scale, calm neutral palette with one trustworthy accent, illustration only in onboarding/empty states. Identity via the hub's tile geometry, illustration style, and accent.
  - selected for: no record matched the wording; best structural fit for platform/product; lexical 0.008, structural 0.64

### Guardrails (must hold)
- **Progress for background work: what, how far, what went wrong** `feedback-progress-async` [feedback/states; heuristic] — State what is happening in words ('Sending 2 of 3 · Photo …'), a determinate bar when the total is known, the current item, elapsed/remaining when useful; on failure name the item and the reason with a Retry action; on completion confirm briefly ('All sent · just now'); keep a stable-phrase live region that announces start, failure and completion once per run; keep the layout stable while the state changes. _(covers: progress indicator, live region status announcements, offline and sync states)_
  - selected for: most relevant record for the request wording
- **Field use: sunlight readability and glanceable status** `mobile-field-use` [accessibility/environment/interaction; heuristic] — Target ≥7:1 text contrast and avoid thin weights and pale tints (glare washes them out); prefer light UI on white or very high-contrast dark, not mid-tone surfaces; make the current state glanceable (large status word plus colour plus icon, readable at arm's length in two seconds); ≥48 dp targets with ≥12 dp spacing and no precision gestures when gloves are likely; put the next action in thumb reach; large numerals for counts and readings; keep the screen usable one-handed and interruptible (state survives backgrounding). _(covers: high contrast outdoors / sunlight readability, glanceable status, large touch targets (≥44–48 px))_
  - selected for: required coverage: accessibility, environment, interaction, glanceable status, high contrast outdoors / sunlight readability, large touch targets (≥44–48 px)
- **Mobile: keyboard and input types** `mobile-keyboard-ime` [component/feedback/interaction; platform-standard] — Set keyboard type and autocomplete/textContentType/autofillHints per field, return key action (Next/Done), scroll the focused field above the keyboard, keep the primary action reachable while the keyboard is open (or on the keyboard toolbar), and dismiss on tap outside for non-modal forms. _(covers: on-screen keyboard (IME) aware layout)_
  - selected for: required coverage: component
- **Offline, sync, and connectivity states** `states-offline-and-sync` [environment/feedback/states; engineering-practice] — Design the offline state as a first-class state: show connectivity clearly but calmly (a persistent status strip, not a blocking modal), keep already-loaded content usable, queue writes locally with a visible 'pending sync' marker per item and a 'last synced' timestamp, retry automatically with backoff and let the user retry manually, never lose entered data, and resolve conflicts explicitly (show both versions or last-writer-wins with an undo). Reads: stale data is labelled with its age. _(covers: offline and sync states, saving, saved and conflict states, last-updated / refresh state)_
  - selected for: recommended coverage: last-updated / refresh state, saving, saved and conflict states
- **Mobile: safe areas and system insets** `mobile-safe-areas` [adaptive/structure; platform-standard] — Content respects safe-area insets (SwiftUI safeAreaInset / .ignoresSafeArea only for backgrounds, Compose WindowInsets + edge-to-edge, RN SafeAreaView/useSafeAreaInsets, web env(safe-area-inset-*)); bottom actions sit above the home indicator/gesture bar; keyboard (IME) insets push the focused field into view. _(covers: safe areas and notches)_
  - selected for: recommended coverage: adaptive, safe areas and notches
Filtered out: anti-hero-template (platform ['web'] not in request ['mobile']); anti-generic-sidebar-dashboard (platform ['desktop', 'web'] not in request ['mobile']); anti-hover-only-actions (platform ['desktop', 'web'] not in request ['mobile']); comp-sidebar-nav (platform ['desktop', 'tablet', 'web'] not in request ['mobile']); comp-tv-sign-in (platform ['tv'] not in request ['mobile']); comp-plan-comparison (platform ['desktop', 'web'] not in request ['mobile'])

Concept trace (explain): covered feedback.progress_indicator, a11y.live_status, state.offline_sync, touch.minimum_target, env.outdoor_readability, env.glanceable_status, touch.safe_areas, data.refresh_timestamp, state.saving_conflict
  - UNCOVERED a11y.accessible_names: candidates existed but the bundle cap or a lower utility left them out (candidates comp-photo-capture-field, a11y-native-semantics, a11y-labels-names)
  - UNCOVERED a11y.contrast: candidates existed but the bundle cap or a lower utility left them out (candidates a11y-contrast-text, anti-fashion-over-usability, a11y-nontext-contrast)
  - UNCOVERED state.loading_empty_error: candidates existed but the bundle cap or a lower utility left them out (candidates comp-chart-container, layout-states-empty-loading-error, anti-no-states)
  - UNCOVERED process.reuse_first: candidates existed but the bundle cap or a lower utility left them out (candidates impl-reuse-before-new)
  - UNCOVERED touch.gestures_discoverable: candidates existed but the bundle cap or a lower utility left them out (candidates mobile-gestures-discoverable, interaction-drag-drop, comp-list-row-mobile)
  - UNCOVERED touch.thumb_reach: candidates existed but the bundle cap or a lower utility left them out (candidates mobile-thumb-reach, anti-mobile-desktop-shrunk, cta-sticky-bar)
  - UNCOVERED navigation.platform_grammar: candidates existed but the bundle cap or a lower utility left them out (candidates mobile-platform-navigation, nav-bottom-tabs)
