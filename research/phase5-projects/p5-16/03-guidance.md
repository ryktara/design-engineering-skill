## guidance: Dispatchers want to see which vehicles are overdue for service without opening each one.
status=ABSTAIN mode=['audit', 'refactor'] platform=['desktop'] input=['keyboard', 'pointer'] product=[] screen=[] stack=['avalonia'] density=None env=[] risk=low negatives=[]
OUT_OF_SCOPE (UNKNOWN): no UI vocabulary found; not a UI design task as written. Nearest supported UI task: describe the screen, component, or user-facing problem.
